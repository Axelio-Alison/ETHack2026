# Core tables and feature aggregation
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2

# Loaded lazily only when real model inference is requested
torch>=2.2
transformers>=4.44,<5
sentence-transformers>=3,<7
